# Placeholder secret (e.g. a Redis password or data-vendor token). Values are
# base64-encoded. In real clusters use a sealed-secret / external-secrets, not git.
apiVersion: v1
kind: Secret
metadata:
  name: volatility-secret
  labels: {app: volatility-api}
type: Opaque
data:
  # echo -n "changeme" | base64
  API_TOKEN: Y2hhbmdlbWU=
